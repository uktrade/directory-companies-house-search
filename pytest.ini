[pytest]
DJANGO_SETTINGS_MODULE = conf.settings
addopts = --ignore=node_modules --capture=no -Wignore::DeprecationWarning -vv

markers = 
    opensearch_test_data: Tests that require data from open search db